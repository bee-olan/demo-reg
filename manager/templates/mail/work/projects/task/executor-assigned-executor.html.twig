{% extends 'mail.html.twig' %}

{% block body %}

    <p>Hello, {{ executor.name.first }}!</p>

    <p>Вам поручено тестировать матку &laquo;{{ task.name }}&raquo;:</p>

    {% set url = url('work.projects.tasks.show', {'id': task.id}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>

{% endblock %}